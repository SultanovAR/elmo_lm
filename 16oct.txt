16 октября

Журнал:
    сделал аугментацию с помощью elmo_lm появились 2 вопроса

Задача А: Необходимо решить как взаимодействовать с двумя распределениями левым и правым
    1) усреднять
    2) усреднять с весами в зависимости от положения слова(если 2/22 то правый контекст важнее чем левый, вроде)
    3) выбрать один контекст либо левый либо правый
    4) смотреть на распределения может одно из них слишком размазано исходя из чего можно сказать что оно в затруднительном
        положении по выбору слова
    5) взять слова и из левого и из правого, без выбора что важнее
    
    Предполагаемое решение:
    1)Для каждого метода взять нарисовать гистограммы распредлений 10 слов наиболее вероятных 1 каждого из способов смешивания для
    предложения: "Almost half of all iPhone owners have broken their screens, not just once but an average of two times each."
    после чего эмпирически оценить каждый из методов.
    2)Закинуть выбор как гиперпараметр, но +1 гиперпараметр и еще не понятно как между ними выбирать.
    

    
Задача B: Необходимо решить как ограничить заменяемые слова и вообще необходимо ли ограничивать
    1) ограничения по частям речи
    2) ограничения по распределению если уверенность модели языка меньше порога то не рассматривать это слово
    3) частота замены слов
    4) не ограничивать, но тогда предложение может потерять смысл
    5) Если слово неизвестно elmo_lm то наверно не стоит его заменять?
    
    Предполагаемое решение:
    1) Не знаю
    2) взять каждый и такой посомтреть что получается на предложении "Almost half of all iPhone owners have broken their screens,
    not just once but an average of two times each." и снова эмпирически прикинуть что работает что нет {фигня(((}
    

исход модели на текущее время:
    'Исходное', 'Новое'
    ('Almost', 'about')
    ('half', 'all')
    ('of', 'of')
    ('all', 'all')
    ('iPhone', 'iPhone')
    ('owners', 'users')
    ('have', 'has')
    ('broken', 'off')
    ('their', 'the')
    ('screens', 'screens')
    (',', 'to')
    ('not', 'not')
    ('just', 'just')
    ('once', ',')
    ('but', 'but')
    ('an', 'for')
    ('average', 'maximum')
    ('of', 'of')
    ('two', '10')
    ('times', 'years')
    ('each', 'per')
    ('.', '.')
    A) Модель усредняет распределения
    B) Из ограничений только частота замены равная 0.7 

